{% comment %}
  Announcement / marquee bar REMOVED per request (2026-08-18). The header now
  floats as a pill over the hero, and the below-hero trust marquee was dropped
  from the homepage. This section renders nothing; the original markup is kept
  below (inside a comment) so it can be restored if ever needed.
{% endcomment %}
{% comment %}
{%- assign show_promo = false -%}
{%- if template.suffix == 'lp-caffeine-alt' -%}
  {%- assign show_promo = true -%}
  {%- assign promo_label = 'LIMITED TIME:' -%}
  {%- assign promo_text = '15% Off with Code' -%}
  {%- assign promo_code = 'WELCOME15' -%}
{%- endif -%}
  (original announcement-bar markup preserved — the full ann_items capture and
  .announcement-bar / .announcement-track structure. Restore from git history
  commit ff7f2f8 or earlier if this bar is wanted again.)
{% endcomment %}

{% schema %}
{
  "name": "Announcement Bar",
  "settings": []
}
{% endschema %}
